version: '3.9'

services:
  frontend:
    build: ./frontend
    ports:
      - '5173:5173'
    volumes:
      - ./frontend/src:/app/src   # hot-reload in dev
    env_file: .env
    depends_on:
      - backend

  backend:
    build: ./backend
    ports:
      - '3001:3001'
    env_file: .env
