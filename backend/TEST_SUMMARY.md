# Test Summary — Unit Tests

สรุปการทดสอบระดับ Unit (service layer) ที่เตรียมไว้เพื่อใส่ในรายงาน

## วัตถุประสงค์
- ยืนยัน business logic ของ `FirewallService` และ `FloodService` โดย mock ขาออก (repositories, IPC)
- ตรวจ validation, error paths และการเรียก `ipc.send()` เมื่อจำเป็น

## คำสั่งเพื่อ reproduce

```bash
cd backend
npm install
npx jest --runInBand --coverage
# รายงาน coverage HTML: backend/coverage/lcov-report/index.html
```

## ตารางการทดสอบ

| Component | Function | กรณีที่ทดสอบ (Test cases) | วัตถุประสงค์ | ผลลัพธ์ที่คาดหวัง |
|---|---|---:|---|---|
| FirewallService | `getFeatures()` | คืนค่า map จาก repository | ยืนยันการดึงสถานะ feature flags | คืนค่า map ที่ถูกส่งกลับ |
| FirewallService | `toggleFeature(feature, enabled)` | valid feature / invalid feature | ตรวจการตั้งค่า flag และการเรียก IPC | เรียก `setEnabled` และ `ipc.send`, invalid -> throw |
| FirewallService | `addToBlacklist(ip, note)` | valid IP / invalid format / duplicate | ตรวจ validate IP และแปลง error ของ repo | สร้างรายการใหม่ + `ipc.send`, invalid/duplicate -> throw |
| FirewallService | `removeFromBlacklist(id)` | id exists / id not found | ตรวจลบ entry และ sync IPC | เรียก `deleteById` และ `ipc.send`, not-found -> throw |
| FirewallService | `addPort(port, note)` | valid port / out-of-range / duplicate | ตรวจ validate port range และ sync | สร้างรายการ + `ipc.send`, invalid/duplicate -> throw |
| FirewallService | `removePort(id)` | id exists / id not found | ตรวจลบ port entry และ sync | เรียก `deleteById` และ `ipc.send`, not-found -> throw |
| FloodService | `getAllFloodConfigs()` | มี rates + feature flags | รวมข้อมูล rates กับ flags | คืน array ของ config ที่มี enabled flag |
| FloodService | `getFloodConfig(type)` | valid/invalid / not found | ตรวจ validation flood type และการคืนค่า | valid -> object, invalid -> throw, not found -> null |
| FloodService | `setFloodEnabled(type, enabled)` | valid/invalid | เปลี่ยนสถานะ flag และส่ง IPC | เรียก `setEnabled` และ `ipc.send`, invalid -> throw |
| FloodService | `updateFloodRates(type, soft, hard)` | invalid types / soft>=hard / negative limits | ตรวจ validation ของ limits | update repo + `ipc.send`, invalid -> throw |
| FloodService | `addFloodLog(type, ip, event)` | invalid type / invalid event / valid | ตรวจ valid events และบันทึก log | คืน id ของ log, invalid -> throw |

## ขอบเขตและข้อจำกัด
- Coverage นี้เป็นระดับ Unit (service layer) เท่านั้น: ไม่รวม repository (DB) และ IPC จริง
- เพื่อยืนยัน end-to-end จำเป็นต้องเขียน Integration tests และ eBPF functional tests เพิ่มเติม

---
Generated by test runner; รายละเอียดไฟล์ทดสอบ: `backend/__tests__/FirewallService.test.js`, `backend/__tests__/FloodService.test.js`.
